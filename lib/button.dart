import 'package:flutter/material.dart';

class button extends StatelessWidget {
  button({super.key, this.backgroundColor, this.color});
  Color? backgroundColor;
  Color? color;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
